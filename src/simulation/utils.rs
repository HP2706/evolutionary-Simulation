use crate::simulation::types::RoundState;

/* pub fn read_json(filename : &str) -> Result<Vec<RoundState>, std::io::Error> {
    let contents = std::fs::read_to_string(filename)?;
    let roundstates: Vec<RoundState> = serde_json::from_str(&contents)?;
    println!("Read {} roundstates from file", roundstates.len());
    Ok(roundstates)
}
*/